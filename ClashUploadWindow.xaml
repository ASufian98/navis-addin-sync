<Window x:Class="NavisWebAppSync.ClashUploadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload Clash Report"
        Height="550" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Upload Clash Detection Report"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#333333"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   Text="Select a category and choose the HTML report file to upload:"
                   Foreground="#666666"
                   Margin="0,0,0,15"/>

        <!-- File Selection -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="FilePathTextBox"
                     Grid.Column="0"
                     Height="35"
                     VerticalContentAlignment="Center"
                     Padding="10,0"
                     IsReadOnly="True"
                     Background="White"
                     BorderBrush="#CCCCCC"/>

            <Button x:Name="BrowseButton"
                    Grid.Column="1"
                    Content="Browse..."
                    Width="100"
                    Height="35"
                    Margin="10,0,0,0"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- Category Label -->
        <TextBlock Grid.Row="3"
                   Text="Select Clash Category:"
                   FontWeight="SemiBold"
                   Foreground="#333333"
                   Margin="0,0,0,8"/>

        <!-- Category List -->
        <Border Grid.Row="4"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                Background="White">
            <ListBox x:Name="CategoryListBox"
                     BorderThickness="0"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10,8">
                            <TextBlock Text="{Binding DisplayName}"
                                       FontWeight="SemiBold"
                                       FontSize="14"/>
                            <TextBlock Text="{Binding Description}"
                                       Foreground="#666666"
                                       FontSize="12"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Optional Fields -->
        <StackPanel Grid.Row="5" Margin="0,15,0,0">
            <TextBlock Text="Report Name (optional):"
                       Foreground="#666666"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="ReportNameTextBox"
                     Height="30"
                     Padding="8,0"
                     VerticalContentAlignment="Center"
                     Background="White"
                     BorderBrush="#CCCCCC"/>
        </StackPanel>

        <!-- Error Message -->
        <TextBlock x:Name="ErrorMessage"
                   Grid.Row="6"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Margin="0,10,0,0"/>

        <!-- Progress Panel -->
        <StackPanel x:Name="ProgressPanel"
                    Grid.Row="6"
                    Visibility="Collapsed"
                    Margin="0,10,0,0">
            <TextBlock x:Name="ProgressText"
                       Text="Uploading..."
                       Foreground="#666666"
                       Margin="0,0,0,5"/>
            <ProgressBar x:Name="UploadProgressBar"
                         Height="20"
                         IsIndeterminate="True"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="7"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="80"
                    Height="35"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="UploadButton"
                    Content="Upload"
                    Width="100"
                    Height="35"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="SemiBold"
                    IsEnabled="False"
                    Click="UploadButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
