<Window x:Class="NavisWebAppSync.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login to BINA Cloud"
        Height="380" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Environment -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Login to BINA Cloud"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#333333"/>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="Environment:"
                           FontSize="12"
                           Foreground="#666666"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="EnvironmentText"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="#4CAF50"
                           Margin="5,0,10,0"
                           VerticalAlignment="Center"/>
                <CheckBox x:Name="UseStagingCheckbox"
                          Content="Use Staging"
                          FontSize="11"
                          Foreground="#888888"
                          VerticalAlignment="Center"
                          Checked="UseStagingCheckbox_Changed"
                          Unchecked="UseStagingCheckbox_Changed"/>
            </StackPanel>
        </StackPanel>

        <!-- Email -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailTextBox"
                     Height="35"
                     Padding="10,8"
                     FontSize="14"
                     BorderBrush="#CCCCCC"
                     BorderThickness="1"/>
        </StackPanel>

        <!-- Password -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox"
                         Height="35"
                         Padding="10,8"
                         FontSize="14"
                         BorderBrush="#CCCCCC"
                         BorderThickness="1"/>
        </StackPanel>

        <!-- Error Message -->
        <TextBlock x:Name="ErrorMessage"
                   Grid.Row="4"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Margin="0,0,0,10"/>

        <!-- Loading Indicator -->
        <StackPanel x:Name="LoadingPanel"
                    Grid.Row="5"
                    Orientation="Horizontal"
                    Visibility="Collapsed"
                    Margin="0,0,0,10">
            <TextBlock Text="Logging in..." Foreground="#666666"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="7"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="80"
                    Height="35"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="LoginButton"
                    Content="Login"
                    Width="80"
                    Height="35"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
