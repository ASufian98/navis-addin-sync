# BINA Navis Sync — LOCAL/DEV (Debug builds embed this)
# For local development with ngrok or localhost backend.

# BINA Cloud API (bina-be)
API_BASE_URL=http://localhost:3000

# OTA update feed (version.json). Empty = updater disabled.
UPDATE_FEED_URL=

# Web origin for login flow
CLOUD_WEB_URL=http://localhost:3001
